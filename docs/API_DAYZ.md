# API DayZ — LEGADO

Estas rotas permanecem no site somente por compatibilidade. O mod RAID-Z Store V74 **não usa HTTP/API** para entregas, VIP, seguro ou recompensa por tempo.

A integração atual está documentada em `FILE_BRIDGE_FTP.md`.
